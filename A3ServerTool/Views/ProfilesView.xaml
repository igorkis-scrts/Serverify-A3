<UserControl x:Class="A3ServerTool.Views.ProfilesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:A3ServerTool.Views"
             xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:dialog="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
             dialog:DialogParticipation.Register="{Binding}"
             mc:Ignorable="d"
             DataContext="{Binding Profiles, Source={StaticResource Locator}}"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Label Grid.Row="0"
               Grid.Column="0"
               Grid.ColumnSpan="2"
               HorizontalAlignment="Left"
               Content="Profiles"
               Style="{DynamicResource DescriptionHeaderStyle}" />

        <DataGrid x:Name="ProfileDataGrid"
                  Grid.Row="1"
                  Grid.Column="0"
                  Grid.ColumnSpan="2"
                  Grid.RowSpan="1"
                  Margin="5"
                  AutoGenerateColumns="False"
                  HeadersVisibility="All"
                  ItemsSource="{Binding Path=Profiles}"
                  RowHeaderWidth="20"
                  SelectionUnit="FullRow">
            <DataGrid.Columns>
                <!--<DataGridTextColumn Binding="{Binding Profile}" Header="Name" />-->
                <DataGridTextColumn Binding="{Binding Name}" Header="Name" Width="Auto" />
                <DataGridTextColumn Binding="{Binding Type}" Header="Game Type" />
                <DataGridTextColumn Binding="{Binding Description}" Header="Description" />
            </DataGrid.Columns>
            <DataGrid.Style>
                <Style BasedOn="{StaticResource MetroDataGrid}" TargetType="{x:Type DataGrid}">
                    <!--<Setter Property="AlternatingRowBackground" Value="{DynamicResource GrayBrush10}" />-->
                </Style>
            </DataGrid.Style>
            <!--<DataGrid.RowStyle>
                <Style BasedOn="{StaticResource MetroDataGridRow}" TargetType="{x:Type DataGridRow}">
                    <Style.Triggers>
                        -->
            <!--  IsMouseOver  -->
            <!--<MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Price, Mode=OneWay, Converter={StaticResource AlbumPriceIsTooMuchConverter}}" Value="True" />
                                <Condition Binding="{Binding Path=IsMouseOver, RelativeSource={RelativeSource Self}}" Value="true" />
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Background" Value="#FFBDBD" />
                        </MultiDataTrigger>-->
            <!--
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>-->
        </DataGrid>
        <Grid Grid.Row="2"
              Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="0.5*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Button Style="{DynamicResource MetroFlatButton}" Grid.Row="0" Grid.Column="0" MinWidth="80" Height="28" HorizontalAlignment="Center" Margin="{StaticResource ControlMargin}">
                <DockPanel>
                    <iconPacks:PackIconMaterial Kind="Check" DockPanel.Dock="Left"></iconPacks:PackIconMaterial>
                    <TextBlock Margin="4 0 0 0" ><Bold>Choose</Bold></TextBlock>
                </DockPanel>
            </Button>

            <Button Style="{DynamicResource MetroFlatButton}" Grid.Row="1" Grid.Column="0" MinWidth="80" Height="28"
                    HorizontalAlignment="Center" Margin="{StaticResource ControlMargin}" Content="Create"
                    Command="{Binding CreateProfileCommand}">
            </Button>
            <Button Style="{DynamicResource MetroFlatButton}" Grid.Row="1" Grid.Column="1" MinWidth="80" Height="28"
                    HorizontalAlignment="Center" Margin="{StaticResource ControlMargin}" Content="Edit">
            </Button>
            <Button Style="{DynamicResource MetroFlatButton}" Grid.Row="1" Grid.Column="2" MinWidth="80" Height="28"
                    HorizontalAlignment="Center" Margin="{StaticResource ControlMargin}" Content="Delete">
            </Button>
        </Grid>
    </Grid>
</UserControl>

